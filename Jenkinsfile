pipeline {
    agent any
    stages {
        stage('Run Playwright Tests') {
            steps {
                script {
                    sh 'npx playwright test test1' // Replace with the actual path if different
                }
            }
        }
    }
}
